= provide(:title, Bid.model_name.human)
%h1= Bid.model_name.human

%div.row
	%div.span6.offset3
		= semantic_form_for([@auction, @bid]) do |f|
			= render 'fields', f: f

= link_to t(:back), auction_bids_path(@auction)

